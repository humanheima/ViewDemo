# Codex 新手使用指南

Codex 是一个可以阅读项目、修改代码、运行命令和协助排查问题的编程助手。使用它时，最重要的是把目标、范围和验收标准说清楚。

## 1. 第一次使用

打开项目目录后，先让 Codex 了解项目：

```text
先理解一下这个项目：说明项目结构、启动入口、主要模块和构建方式，不要修改代码。
```

然后可以继续询问：

```text
这个项目中和软键盘相关的代码在哪里？请列出入口 Activity、核心类和对应布局。
```

## 2. 一个好的请求包含什么

尽量包含四件事：

1. 目标：想实现或解决什么问题。
2. 范围：允许修改哪些模块或文件。
3. 约束：必须遵守的技术方案、兼容版本或代码风格。
4. 验收标准：完成后应该看到什么结果。

示例：

```text
请修复 app 中列表滚动时偶发崩溃的问题。
先定位原因，再修改代码；只改 app 模块，不要改 pullrefresh。
保持 Kotlin 现有风格，并补一个可以覆盖这个场景的测试。
最后运行相关测试并说明结果。
```

## 3. 常用请求模板

### 理解代码

```text
请解释这个文件的职责、主要流程、关键状态和它依赖的类，不要修改代码。
```

### 修复问题

```text
运行 [命令]，分析报错原因并修复。
修改范围限制为 [模块/文件]，修复后重新运行验证。
```

### 增加功能

```text
请在 [模块] 中增加 [功能]。
沿用项目已有的 [架构/组件/命名方式]，不要做无关重构。
完成后补测试并运行验证。
```

### 代码审查

```text
请 review [文件或模块]。
优先指出真实 bug、兼容性风险、性能问题和缺失测试，并给出文件和行号。
先不要修改代码。
```

### 重构

```text
请重构 [目标代码]，保持外部行为不变。
先说明风险和计划，再实施；不要顺便修改无关文件。
```

## 4. 让 Codex 修改代码时的工作方式

推荐使用这个顺序：

1. 先让它调查现状和相关代码。
2. 明确修改范围和不应改变的行为。
3. 让它实施修改。
4. 让它运行构建、测试或静态检查。
5. 查看变更摘要和验证结果。

可以直接说：

```text
先检查相关实现和测试，再给出简短计划；确认后直接修改并验证。
```

如果不希望修改文件，要明确写：

```text
只分析，不要修改任何文件。
```

## 5. Android 项目的常用命令

```bash
./gradlew :app:assembleDebug
./gradlew :app:test
./gradlew :app:lint
./gradlew projects
```

让 Codex 运行命令时，最好指定范围：

```text
请运行 ./gradlew :app:assembleDebug；如果失败，定位第一个真正的错误，不要只总结最后一行。
```

## 6. 如何提供有效上下文

- 给出具体文件、类名、日志和复现步骤。
- 说明这是新功能、回归问题还是兼容性问题。
- 说明是否允许新增依赖。
- 说明目标 Android 版本、设备或构建变体。
- 大任务拆成几个可验证的小任务。

不太有效的请求：

```text
帮我优化一下代码。
```

更有效的请求：

```text
请分析 MainActivity 的启动耗时，优先检查同步初始化和磁盘 I/O。
保持功能不变，给出可量化的优化建议；确认后再修改。
```

## 7. 审查改动

每次修改后都应关注：

- 修改了哪些文件。
- 是否引入了不相关变更。
- 是否覆盖空数据、异常、生命周期和线程场景。
- 测试是否真的运行成功。
- 是否需要手动在模拟器或真机验证。

可使用：

```bash
git diff
git status --short
```

## 8. 何时需要你确认

涉及删除文件、升级大量依赖、修改发布配置、提交或推送 Git、访问外部服务时，应让 Codex 先说明影响并等待确认。普通的代码阅读、局部修改和项目内测试通常可以直接执行。

## 9. 最实用的一句话

```text
先读相关代码，说明你的判断和修改范围；然后实施最小改动，运行合适的验证，并告诉我改了什么、验证了什么、还剩什么风险。
```
